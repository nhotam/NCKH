- Các thư viện cần cài đặt (cài đặt trong Terminal trên IDE hoặc Terminal bên ngoài máy tính):
      Langchain: pip install langchain langchain-community langchain-core requests
- Cách test code:
  B1: Mở terminal
  B2: Trỏ đến thư mục chứa code: cd .../NCKH/RAG (đưa đường dẫn file vào sau cd để có thể trỏ đến thư mục code)
  B3: Chạy code: python file_name
    Chú ý:
    *Đối với lần chạy đầu tiên: Phải chạy trước file build_db.py
    *Sau đó, chạy file chatbot.py 
    *Từ lần thứ 2 trở đi thì không cần chạy file build_db.py nữa.
